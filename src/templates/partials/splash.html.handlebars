<div class="section black-text" id="index-banner">
  <div class="container">
    <h2 class="header thin center black-text">
      {{#if currentCity.isFlying}}
        Dave is flying to <span class="current-city">{{currentCity.name}}</span>
      {{else}}
        Dave is in <span class="current-city">{{currentCity.name}}</span>
      {{/if}}
    </h2>
    <div class="row center">
      <div class="col s12">
        <p class="flow-text">
          Welcome back to the blog. This time my brother and I are taking on New York City.
        </p>
        <p class="flow-text">
          Click the button below to see the latest post, or pick a specific day from the menu by clicking the hamburger menu at the top left.
        </p>
      </div>
    </div>
    <div class="row center">
      <a href="cali/day-nine" id="download-button" class="btn-large waves-effect waves-light black amber-text">See What I'm Up To!</a>
    </div>
  </div>
</div>
